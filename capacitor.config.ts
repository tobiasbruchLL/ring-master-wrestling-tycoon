import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nightskygames.ringmaster',
  appName: 'Ring Master - Wrestling Tycoon',
  webDir: 'dist'
};

export default config;
